{
  "name": "iw4madmin-population-notifier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IW4MAdmin plugin that sends Discord alerts as servers fill up",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/watch.mjs",
    "mock:dashboard": "node scripts/mock-dashboard.mjs",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3",
    "@types/node": "^24.5.2"
  }
}
